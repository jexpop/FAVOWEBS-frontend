import { ArraySortedPipe } from './array-sorted.pipe';

describe('ArraySortedPipe', () => {
  it('create an instance', () => {
    const pipe = new ArraySortedPipe();
    expect(pipe).toBeTruthy();
  });
});
